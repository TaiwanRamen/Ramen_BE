const userRepository = require('../repository/notification.repository'),
    log = require('../modules/logger');

const notificationService = {}

notificationService.signToken = async (notification) => {

}
module.exports = notificationService;